<template>
    <div class="about">
        <h1>About this demo</h1>

        <p>
            This is the runnable example for <code>nexus/live/template</code>.
            Open the dev tools network tab — every interaction flows over a
            single WebSocket, and the server only ships sparse diffs.
        </p>

        <p>
            <a nl-navigate href="/">Back to Posts</a>
        </p>

        <h2>What you're seeing</h2>
        <ul>
            <li>Server-side rendered first paint (view source on either page).</li>
            <li>Live two-tab sync via the shared <code>PostsRepo</code> +
                <code>nexus.Notifier</code>.</li>
            <li>Per-session state preserved across brief disconnects
                (<code>WithSessionResumption</code>).</li>
            <li>This page → Posts transition stays inside the existing WS
                connection — no full reload (<code>nl-navigate</code>).</li>
        </ul>
    </div>
</template>

<style scoped>
    .about {
        font-family: system-ui, sans-serif;
        max-width: 36rem;
        margin: 2rem auto;
        line-height: 1.5;
    }

    .about h1 {
        font-size: 1.5rem;
    }

    .about h2 {
        font-size: 1.1rem;
        margin-top: 2rem;
    }

    .about code {
        background: #f4f4f4;
        padding: 0 0.25rem;
        border-radius: 3px;
        font-family: ui-monospace, monospace;
        font-size: 0.9em;
    }

    .about a {
        color: #2a64d6;
    }
</style>
