<template>
    <div class="posts">
        <h1>Posts ({{ len(Posts) }})</h1>

        <input nl-model="Filter" placeholder="Filter by title…"/>

        <ul>
            <PostRow nl-for="p in Posts" :key="p.ID" :post="p"/>
        </ul>

        <p nl-if="len(Posts) == 0">No posts match "{{ Filter }}".</p>

        <form @submit.prevent="add">

            <input nl-model="NewTitle" placeholder="New post title…"/>
            <button type="submit">Add</button>
        </form>

        <p class="hint">
            Open this URL in another tab — like, add, or filter in one and the
            others stay in sync via the Notifier + sparse diffs.
        </p>
    </div>
</template>

<style scoped>
    .posts {
        font-family: system-ui, sans-serif;
        max-width: 36rem;
        margin: 2rem auto;
    }

    .posts h1 {
        font-size: 1.5rem;
    }

    .posts input {
        width: 100%;
        padding: 0.5rem;
        margin: 0.5rem 0;
        box-sizing: border-box;
    }

    .posts ul {
        list-style: none;
        padding: 0;
    }

    .posts li {
        display: flex;
        gap: 0.75rem;
        align-items: center;
        padding: 0.5rem 0;
        border-bottom: 1px solid #eee;
    }

    .posts li strong {
        flex: 1;
    }

    .posts li span {
        color: #888;
        font-size: 0.85rem;
    }

    .posts button {
        cursor: pointer;
    }

    .posts form {
        margin-top: 1rem;
        display: flex;
        gap: 0.5rem;
    }

    .posts form input {
        flex: 1;
    }

    .posts .hint {
        color: #888;
        font-size: 0.85rem;
        margin-top: 2rem;
    }
</style>
